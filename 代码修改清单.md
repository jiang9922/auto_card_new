# auto_card 代码修改清单

**日期**: 2026-03-17  
**修改人**: PM AI (已通过本地测试)  
**执行人**: @程序员Claw

---

## 一、Backend/main.go 修改内容

### 1. 新增：查询任务管理（方案3：按需查询）

在文件开头（`httpClient` 变量之后）添加以下代码：

```go
// ==================== 查询任务管理（方案3：按需查询） ====================

// CardInfo 存储查询结果
type CardInfo struct {
	CardNo          string `json:"card_no"`
	CardCode        string `json:"card_code"`
	CardExpiredDate string `json:"card_expired_date"`
	CardNote        string `json:"card_note"`
}

// QueryTask 查询任务状态
type QueryTask struct {
	CardNo     string     `json:"card_no"`
	Status     string     `json:"status"` // querying, completed, failed
	Result     *CardInfo  `json:"result,omitempty"`
	Error      string     `json:"error,omitempty"`
	CreatedAt  time.Time  `json:"created_at"`
	UpdatedAt  time.Time  `json:"updated_at"`
}

// 查询任务缓存（内存存储）
var queryTasks = make(map[string]*QueryTask)
var queryTasksMutex sync.RWMutex

// 获取或创建查询任务
func getOrCreateQueryTask(cardNo string) *QueryTask {
	queryTasksMutex.Lock()
	defer queryTasksMutex.Unlock()
	
	task, exists := queryTasks[cardNo]
	if !exists || time.Since(task.UpdatedAt) > 5*time.Minute {
		task = &QueryTask{
			CardNo:    cardNo,
			Status:    "pending",
			CreatedAt: time.Now(),
			UpdatedAt: time.Now(),
		}
		queryTasks[cardNo] = task
	}
	return task
}

// 更新查询任务状态
func updateQueryTask(cardNo string, status string, result *CardInfo, errMsg string) {
	queryTasksMutex.Lock()
	defer queryTasksMutex.Unlock()
	
	if task, exists := queryTasks[cardNo]; exists {
		task.Status = status
		task.Result = result
		task.Error = errMsg
		task.UpdatedAt = time.Now()
	}
}

// 清理过期的查询任务（超过30分钟）
func cleanupExpiredQueryTasks() {
	queryTasksMutex.Lock()
	defer queryTasksMutex.Unlock()
	
	cutoff := time.Now().Add(-30 * time.Minute)
	for cardNo, task := range queryTasks {
		if task.UpdatedAt.Before(cutoff) {
			delete(queryTasks, cardNo)
		}
	}
}
```

### 2. 修复：batchDelete 函数

**原代码（有bug）**:
```go	tx.Commit()
	stmt.Close()  // 这行是错误的，stmt不存在

	c.JSON(200, Response{Code: 0, Message: "删除成功"})
```

**修改为**:
```go
	if err := tx.Commit(); err != nil {
		c.JSON(500, Response{Code: -1, Message: "提交事务失败"})
		return
	}

	c.JSON(200, Response{Code: 0, Message: fmt.Sprintf("删除成功，共删除 %d 条", deletedCount)})
```

---

## 二、测试用例更新

文件：`test_cases.md`

在 "2.1 用户接口" 部分添加：

| 用例ID | 接口 | 方法 | 测试场景 | 预期结果 | 优先级 |
|--------|------|------|----------|----------|--------|
| API-005 | /api/cards/query?sync=1 | GET | 同步模式查询 | 等待远程查询完成，返回完整数据 | P0 |
| API-006 | /api/cards/status | GET | 轮询查询任务状态 | 返回 {code:0, data:{task_status, card}} | P0 |
| API-007 | /api/cards/status | GET | 查询不存在的任务 | 返回 {code:-1, message:"未找到查询任务"} | P1 |

---

## 三、如何应用修改

### 方式1：直接复制代码（推荐）

```bash
cd /root/.openclaw/workspace/auto_card

# 查看完整修改
git diff origin/main backend/main.go

# 直接推送（如果有权限）
git push origin main
```

### 方式2：生成 Patch 文件

```bash
cd /root/.openclaw/workspace/auto_card
git diff origin/main backend/main.go > /tmp/backend_fix.patch
```

然后在 Railway 或本地应用 patch：
```bash
patch -p1 < /tmp/backend_fix.patch
```

---

## 四、本地测试结果

✅ **编译通过**: `go build -o cards-server main.go`

✅ **API测试通过**:
- 异步查询（有效/无效卡号）
- 同步查询模式 `?sync=1`
- 查询任务状态 `/api/cards/status`
- 登录/添加卡密/短信推送/备份

---

## 五、部署后需要测试的用例

@测试工程师Claw 部署后请测试：

1. **API-001**: 异步查询 `/api/cards/query?card=xxx`
2. **API-005**: 同步查询 `/api/cards/query?card=xxx&sync=1`
3. **API-006**: 查询任务状态 `/api/cards/status?card=xxx`
4. **F-006**: 前端轮询显示"验证中..."
5. **P-005**: 10用户并发查询

---

**修改完成时间**: 2026-03-17 10:30
**等待**: @程序员Claw 推送代码并部署
